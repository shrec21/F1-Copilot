{
  "_comment": "Build config for the Fastify backend. rootDir=src keeps the dist/ layout flat (dist/index.js, not dist/src/index.js). The @f1/rule-engine import resolves via the npm workspace symlink in node_modules/ — the rule-engine package must be compiled before running this build (see 'build' script in package.json).",
  "extends": "./tsconfig.json",
  "include": ["src/**/*"],
  "compilerOptions": {
    "rootDir": "src",
    "paths": {}
  }
}
